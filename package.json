{
  "name": "mihayos-safaris-cloudflare",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --d1=DB --local",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
